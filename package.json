{
  "name": "css-component-library-ssg",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node generate.js",
    "dev": "node server.js",
    "minify": "node minify-build.js",
    "start": "npm run build && npm run dev",
    "deploy": "npm run build && npm run minify && gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "gh-pages": "^6.3.0",
    "glob": "^13.0.0",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.44.1"
  }
}
